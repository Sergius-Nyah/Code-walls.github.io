#include<stdio.h>
void hack(int test_cases, int num_cars)
{
    
void winner(int Car_num)
{ 
int Car[Car_num][3];
for(int i=0;i<Car_num;i++)
{
    printf("---- CAR NUMBER %d ----\n\n", i);
    for(int j=0;j<3;j++)
    {
          printf("Input Car speed, Cs\n");
          printf("Input Turbo speed, Ct\n");
          printf("Input Turbo boost cooldown, Cc\n");
          printf("Input Turbo boost duration time, Cd\n");
          scanf("%d", &Car[i][j]);
          for(int m=0;m<Car_num;m++)
          if(Car[i][j] > Car[i+1][j])  // Compares speed data for i cars, increments point variable.
          {
            int points_0=0;
            points++;
            else 
            int points_1=0;
            points_1++;
          }
          else if(Car[i][j+1] > Car[i+1][j+1])
          {
            int turbo_0=0;
            turbo_0++;
            else 
            int turbo_1=0;
            turbo_1++;
          }
          else if(Car[i][j+2] > Car[i+1][j+1])
    }
} 
if 
}

main()
{

}